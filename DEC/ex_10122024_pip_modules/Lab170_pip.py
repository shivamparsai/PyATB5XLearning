# pip - Python Package Installer, this package management system for python
# in terminal we can give the command - python --version, pip --version, pip list,
# pip install <package name>
# The package we need to install as per our requirement, will be found on pypi.org (pip repository)
# multiple packages can be installed at same time - pip install pandas selenium etc
# we can also uninstall the package - pip uninstall pandas
